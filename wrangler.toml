name = "pink"
compatibility_date = "2026-03-09"

# Frontend deployment target (Cloudflare Pages)
pages_build_output_dir = "dist"

[vars]
APP_ENV = "production"

# Map these in Cloudflare dashboard/secrets for backend container runtime.
# SUPABASE_URL = ""
# SUPABASE_ANON_KEY = ""
